spring.profiles.active: local

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: weshlist
# Logging
logging:
  group:
    framework: org.springframework
    library: org.apache, io.netty, org.mongodb, de.flapdoodle.embed
    project: io.weshlist.minutemaid
  level:
    framework: INFO
    library: INFO
    project: DEBUG